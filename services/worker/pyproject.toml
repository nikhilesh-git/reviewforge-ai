[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "worker"
version = "0.1.0"
description = "Celery worker and LangGraph orchestrator for the GitHub PR Code Reviewer"
requires-python = ">=3.10"
dependencies = [
    "pr-reviewer-shared",
    "celery[redis]>=5.4.0",
    "langgraph>=0.2.28",
    "langchain-openai>=0.1.25",
    "langchain-core>=0.2.38",
    "langfuse>=2.36.0",
    "httpx>=0.27.2",
    "structlog>=24.4.0",
    "pydantic>=2.8.0",
    "pydantic-settings>=2.4.0",
    "tenacity>=9.0.0",
    "qdrant-client>=1.10.1",
    "sentence-transformers>=3.0.0",  # Free local embeddings (all-MiniLM-L6-v2, no API needed)
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.3",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=5.0.0",
    "pytest-mock>=3.14.0",
    "ruff>=0.5.7",
    "black>=24.8.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
